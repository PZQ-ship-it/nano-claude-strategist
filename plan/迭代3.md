🗺️ 迁移迭代 3：详细实施方案科学计算底座建设 (skill/strategy/or_math.py)：提取并复刻 Nash-Copilot 中的数学引擎，确保数学计算逻辑纯粹、无状态且与大模型业务完全解耦。蒙特卡洛模拟 (Monte Carlo)：基于 Beta-PERT 分布的概率密度，使用 scipy.stats.beta.rvs 将三点估算（Min, Mode, Max）转化为 Beta 分布，利用 numpy 进行 10,000 次极速向量化抽样。计算期望效用 (EU) 的平均值、95% 置信区间下限 (极端下行风险) 和上限 (极端红利)。夏普利值计算 (Shapley Value)：实现合作博弈论的经典算法，利用组合数学 (itertools.combinations 和 math.factorial) 遍历特征函数计算边际贡献，得出绝对公平的分润金额。扩展数据契约 (skill/strategy/schemas.py)：在迭代 2 的基础上，新增用于博弈论协作评估的 CooperationContext(BaseModel) 数据模型。包含字段：rationale (协作背景), players (参与方), standalone_values (单干机会成本), synergy_value (协同总盘子), cooperation_cost (合作摩擦成本)。工具层升级与新增 (skill/strategy/tools.py)：升级原有 Tool：将迭代 2 中的 evaluate_strategic_options 工具的内部计算逻辑，从“简单均值公式”升级为调用 or_math.run_monte_carlo_eu，并在返回给 LLM 的 Markdown 报告中强制暴露 95% 置信区间。新增博弈论 Tool：封装新的 evaluate_cooperation_synergy 工具。同样遵循“拦截器生命周期”：LLM 传参 $\rightarrow$ TUI 全屏审计 $\rightarrow$ 人类放行 $\rightarrow$ 调用 Shapley 数学引擎计算 $\rightarrow$ 结合协同增量 (Synergy Surplus) 输出分润报告。系统级 Prompt 终极强化：扩展 nano-claude-code 中的系统提示词。指导 LLM 必须使用夏普利值工具来“从数学上终结囚徒困境与分润谈判”，并要求其在输出战略建议时，必须向用户警示置信区间的下限（防守底线）。🧪 迭代 3 验收场景测试方案 (UAT Plan)完成代码修改并启动终端后，请进行以下硬核测试：测试场景您的输入指令 / 预期行为验证核心点场景一：蒙特卡洛风控模拟输入：“我在考虑两个创业方案。A方案稳扎稳打，B方案高风险高回报。请调用工具做三点估算和期望效用评估。”预期：TUI 拦截审查通过后，LLM 的最终回复中明确提到了 “经过万次蒙特卡洛模拟”，并给出了 95% 置信区间下限，同时对高风险方案发出可能面临巨额亏损的警告。1. numpy 的向量化计算是否极速完成（无卡顿）。2. 大模型是否遵循了强调下行风险的 Prompt 约束。场景二：夏普利值博弈分配输入：“我和朋友合伙开公司。我自己干能赚100万，他自己干赚80万。我们合作预计能做大到300万，但有20万的磨合成本。请用工具计算最公平的分润比例，严禁五五开。”预期：LLM 触发新工具，TUI 弹出让您核对单干成本与协同总盘。保存放行后，LLM 输出精确的 Shapley Value 金额（A:150万, B:130万），拒绝平分逻辑，并解释了“协同增量”。1. 验证 CooperationContext 的 Schema 解析是否正确。2. 夏普利特征函数 v_func 的构造与算法逻辑是否正确。场景三：负协同效应边界防御操作：重新运行上述案例，在 TUI 拦截界面中，手动将 cooperation_cost 改为 150 万（导致总净利 150 万，小于单干之和的 180 万），按 Ctrl+S 放行。预期：工具算出协同增量为负数。LLM 看到计算结果后，强烈建议放弃合作。验证工具在边界条件下的数学鲁棒性，以及 LLM 对“反直觉数学事实”的遵从度。场景四：确定性参数边界容错操作：在战略评估工具的 TUI 阶段，故意将某一方案的 min_val、mode_val、max_val 设为完全相同的数值（例如都是 100）。按 Ctrl+S。验证 or_math.py 中的边界保护逻辑，防止 Beta 分布在 sigma=0（方差为0）或 min==max 时触发除以零导致程序崩溃。